<Window x:Class="LiveChartsDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:viewModels="clr-namespace:LiveChartsDemo.ViewModels"
        xmlns:local="clr-namespace:LiveChartsDemo"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="800" 
        Width="1200">
    <Window.DataContext>
        <viewModels:MainViewModel/>
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" 
                   Text="Демонстрация диаграмм LiveCharts" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="10"/>

        <!-- Гистограмма -->
        <GroupBox Grid.Row="1" Header="1. Гистограмма (Column Chart)" Margin="5">
            <lvc:CartesianChart Series="{Binding ColumnSeries}">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Месяцы" Labels="{Binding MonthLabels}"/>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="Значения" LabelFormatter="{Binding Formatter}"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </GroupBox>

        <!-- Кнопка обновления -->
        <Button Grid.Row="2" Content="Обновить данные" Command="{Binding UpdateDataCommand}" 
                HorizontalAlignment="Center" Width="150" Height="30" Margin="5"/>

        <!-- Линейный график и Круговая диаграмма -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Линейный график -->
            <GroupBox Grid.Column="0" Header="2. Линейный график (Line Chart)" Margin="5">
                <lvc:CartesianChart Series="{Binding LineSeries}">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Месяцы" Labels="{Binding MonthLabels}"/>
                    </lvc:CartesianChart.AxisX>
                </lvc:CartesianChart>
            </GroupBox>

            <!-- Круговая диаграмма -->
            <GroupBox Grid.Column="1" Header="3. Круговая диаграмма (Pie Chart)" Margin="5">
                <lvc:PieChart Series="{Binding PieSeries}"/>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
